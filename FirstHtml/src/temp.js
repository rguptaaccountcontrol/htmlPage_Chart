//var gd = csvdata
        //            .toString() // convert Buffer to string
        //            .split('\n') // split string to lines
        //            .map(e => e.trim()) // remove white spaces for each line
        //            .map(e => e.split(',').map(e => e.trim())); // split each line to array

        //console.log(gd);
        //console.log(JSON.stringify(gd, '', 2)); // as json